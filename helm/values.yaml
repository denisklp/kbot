replicaCount: 1
autoscaling:
  enabled: false
ingress:
  enabled: false
image:
  registry: "docker.io"
  repository: "kbot"
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v25-0dcd0e0"
  os: linux
  arch: amd64
secret:
  name: kbot-secret
  tokenKey: token
  tokenName: TELE_TOKEN
  tokenValue:
service:
  port: 80
securityContext:
  privileged: true
