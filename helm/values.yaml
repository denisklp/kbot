# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: mrfwdmail
  repo: kbot
  tag: "v22-7082fdb"
  #arch: amd64

  os: linux
  arch: amd64
secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
  teleToken: "write your telegram token"
securityContext:
  privileged: true
